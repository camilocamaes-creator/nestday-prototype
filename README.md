# Nestday prototype

Clickable test prototype of a daycare parent-communication web portal (parents, educators, directors).

Everything in it is fake: the centre, the children, the staff and the test logins. The "login" runs in the browser only and is not real security. Test accounts are listed on the sign-in page.

Demo changes are saved only in your own browser; use "Reset demo data" to start over.
